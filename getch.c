#include <stdio.h>
#include <conio.h>

int main() {
    char ch; //Variável caractere
    ch = getch(); //Atribui valor digital no cmd à variável.
    printf("Voce digitou a letra %c", ch);
    return 0;
}